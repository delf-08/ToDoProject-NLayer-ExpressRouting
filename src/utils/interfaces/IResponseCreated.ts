export interface IResponseCreated {
    id: number;
}

/**
 * @swagger
 * components:
 *   schemas:
 *     IResponseCreated:
 *       type: object
 *       properties:
 *         id:
 *           type: integer
 *           description: The unique identifier of the newly created user
 */
